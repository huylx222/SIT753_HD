apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app: web  # Added matching label
spec:
  selector:
    app: web
  ports:
  - port: 80
    targetPort: 3000
  type: LoadBalancer